# PLAN

기술 지식을 마크다운으로 정리해 매일 조금씩 읽는 저장소다. 출퇴근길에 모바일로 한 편씩 읽는 것을 기준으로 쓴다.

---

## spring-backend-issues는 3편으로 닫는다

이 시리즈는 "경계"라는 축으로 짜여 있다. 1편은 브라우저, 2편은 DB, 3편은 외부 세계·런타임과의 경계이고, 3편은 "시리즈를 마치며"로 끝난다. 4편을 붙이려면 네 번째 경계가 필요한데, 남은 후보인 테스트·보안·메시징은 경계가 아니라 주제다. 억지로 이어 붙이면 축이 흐려진다.

대신 형식은 계속 쓴다. "반창고 → 실제로는 → 제대로 → 핵심 한 줄"은 Spring 밖에서도 통하는 틀이라, 주제별 새 시리즈에 그대로 가져간다. 기존 세 편에 빠진 내용이 보이면 편을 늘리지 말고 해당 편에 항목이나 "짧게 짚고 가는 것들"로 넣는다.

---

## 다음 시리즈 (우선순위 순)

우선순위는 설계 판단에 직결되는 순서다. 연차가 쌓일수록 "무엇을 쓸까"보다 "왜 이걸 골랐고 무엇을 포기했나"를 설명하는 힘이 중요해지고, 실무에서 겪은 일관성·멱등성 문제가 곧 설계 논거가 된다.

### 1. `system-design-blocks` — 시스템 설계 부품

- **왜**: 시스템 설계는 부품을 조립하는 일이다. 부품마다 무엇을 얻고 무엇을 포기하는지 즉답할 수 있어야 병목을 먼저 짚는다.
- **형식**: 반창고 틀이 맞지 않는다. 항목마다 "왜 필요한가 → 어떻게 동작하나 → 무엇을 포기하나 → 핵심 한 줄"로 쓴다.
- **구성안**
  - 1편 데이터: 샤딩, 일관된 해싱, 복제와 복제 지연, 분산 ID 생성
  - 2편 트래픽: 캐싱 전략과 캐시 스탬피드, 레이트 리미터 알고리즘, 로드밸런싱, 개략 계산(QPS·저장량 어림)
  - 3편 비동기: 메시지 큐, 전달 보장(at-most/at-least/exactly-once), 백프레셔

### 2. `distributed-consistency` — DB 트랜잭션 하나로 끝나지 않을 때

- **왜**: 주문·재고·결제처럼 여러 시스템에 걸친 작업을 다룬다. spring-backend-issues 2편(트랜잭션·동시성)과 3편(재시도·멱등성)의 다음 단계이고, 실무 경험을 설계 언어로 바꾸는 연습이 된다.
- **형식**: 반창고 틀. 예를 들어 반창고는 "`@Transactional` 안에서 `kafkaTemplate.send()`".
- **후보**: 이중 쓰기 문제와 outbox, `@TransactionalEventListener`의 실행 단계, at-least-once와 소비자 멱등성, Redis 분산 락의 함정, 사가와 보상 트랜잭션, 정산 대사(reconciliation), 순서 보장과 파티션 키

### 3. `java-concurrency-issues` — Java 동시성·JVM

- **왜**: Java 백엔드의 기본기이고, spring-backend-issues에서 스레드풀과 메모리를 다룰 때 바닥에 깔린 내용이다.
- **형식**: 반창고 틀.
- **후보**: `CompletableFuture` 기본 풀이 공용 ForkJoinPool인 문제, `parallelStream`, `volatile`과 가시성, `ConcurrentHashMap`에서 `get` 후 `put`의 경합, 스레드풀 위 `ThreadLocal` 누수, 가상 스레드의 pinning, GC 로그 읽기

### 4. `spring-testing-issues` — 테스트가 거짓말하는 순간들

- **왜**: spring-backend-issues 3편의 "테스트가 통과하게 고치기"를 확장한다. 매일 쓰는 영역인데 가장 대충 넘어가는 곳이다.
- **형식**: 반창고 틀.
- **후보**: 테스트의 `@Transactional` 롤백이 가리는 버그(flush 안 됨, LazyInit 은폐), `@MockBean`과 컨텍스트 캐시로 느려지는 테스트, H2와 운영 DB의 차이와 Testcontainers, 시간 의존 테스트와 `Clock` 주입, 비동기 테스트의 `sleep`

### 5. `spring-security-issues` — AI가 만든 보안 구멍

- **형식**: 반창고 틀.
- **후보**: `@Query` 문자열 결합과 SQL 인젝션, 엔티티를 요청 바디로 바로 받는 mass assignment, 객체 단위 인가 누락(IDOR), JWT 서명·만료 검증, 로그에 남는 개인정보, Actuator 엔드포인트 노출, 설정 파일의 비밀값

---

## 작성 규칙

spring-backend-issues를 다듬으며 정한 규칙이다. 새 시리즈도 따른다.

- **문체**: 본문은 평어체. 인용한 대사는 원래 말투를 살린다.
- **분량**: 한 편에 항목 7~10개. 출퇴근 한 번에 읽히는 양이다.
- **편 구성**: 시리즈 소개 전문은 1편에만 둔다. 모든 편은 항목 묶음을 보여주는 짧은 지도로 시작하고, 교훈 한 문단으로 끝난다.
- **항목 구조**: 반창고 → 실제로는 → 제대로 → 핵심 한 줄. 해법이 짧으면 '실제로는'에 녹인다.
- **제목**: `## N. 주제 — 증상 또는 질문`. 핵심 한 줄이 그 질문에 답한다.
- **핵심 한 줄**: 한 문장.
- **볼드**: 불릿당 최대 1개.
- **불릿**: 세 문장을 넘기면 나누고, 나열은 하위 목록으로 뺀다.
- **사실**: 버전에 따라 달라지는 기본값은 버전을 적는다(예: Boot 3.4부터).
- **파일**: `<시리즈>/<시리즈>-N.md`, 이름은 영어.
